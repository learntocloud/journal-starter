# Database connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=journal
DB_USER=postgres
DB_PASSWORD=change_this_password

# (Optional) Application settings
# APP_ENV=development
# SECRET_KEY=supersecretkey
# LOG_LEVEL=info

# (Optional) For testing
# TEST_DB_NAME=journal_db_test

# (Optional) For Docker Compose, uncomment and set if needed
# POSTGRES_DB=journal
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=postgres
